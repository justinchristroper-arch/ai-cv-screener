# Frontend environment. Copy to `.env` (git-ignored) to override.
#
# Everything here is compiled into the browser bundle and is therefore PUBLIC.
# Never put a secret in a VITE_* variable — provider keys stay server-side.

VITE_API_BASE_URL=http://localhost:8000
