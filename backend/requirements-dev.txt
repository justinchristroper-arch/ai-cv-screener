# Development and test dependencies.
#     python -m pip install -r requirements.txt -r requirements-dev.txt

pytest>=8.3,<9.0
httpx>=0.27,<1.0          # required by starlette's TestClient
ruff>=0.8,<1.0            # linter AND formatter — one tool, not two
