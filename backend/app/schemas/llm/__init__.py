"""LLM structured-output contracts.

Populated in Phase 4 (job description processing). Kept separate from
`schemas/api` because the two families change for different reasons — see
docs/architecture.md section 2.2.
"""
